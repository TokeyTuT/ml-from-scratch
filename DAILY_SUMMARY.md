# 个人每日学习总结

> 项目：`ml-from-scratch` 机器学习算法手写练习  

##  Day 1  — 2026 年 7 月 7 日：项目搭建与 KNN 初步实现

### 今日完成

- 初始化 `ml-from-scratch` 项目，建立 KNN、线性回归、逻辑回归、决策树、朴素贝叶斯、K-Means、PCA 和神经网络等算法目录。
- 补充项目级说明文档、目录说明和 Git 忽略规则，明确项目以手写算法和理解原理为主。
- 从零实现 KNN 分类与 KNN 回归的核心流程：保存训练数据、计算欧氏距离、寻找最近邻，并分别使用多数投票和均值完成预测。
- 使用 NumPy 调整距离计算及近邻索引逻辑，进一步理解 `axis=0` 和 `axis=1` 对数组运算结果的影响。
- 编写基础演示数据，验证 KNN 分类与回归的预测过程。
- 在 `utils/` 中加入数据集划分、Min-Max 归一化和 Z-Score 标准化等预处理工具。
- 准备 Iris 数据集，为后续真实数据实验打下基础。

### 遇到的问题与解决

- 在计算多个样本之间的欧氏距离时，曾因 `axis` 选择不正确导致输出形状不符合预期。通过检查数组的 `(n_samples, n_features)` 结构，明确了需要沿特征维度聚合。
- KNN 分类与回归拥有相同的距离计算和近邻选择流程，但最终聚合方式不同，因此保留了公共思路，同时分别实现投票和均值预测。
- 不同特征的量纲会直接影响距离结果，因此补充了标准化和归一化工具。

### 今日收获

- 理解了 KNN 是一种“惰性学习”算法，训练阶段主要保存数据，计算工作集中在预测阶段。
- 熟悉了 `np.argsort`、`np.argpartition`、`np.unique` 等 NumPy 方法在近邻搜索和类别统计中的用途。
- 认识到数据预处理并不是独立步骤，而是距离类模型能否得到合理结果的重要前提。

### 下一步计划

- 使用 Iris 等真实数据验证 KNN 分类效果。
- 增加回归数据实验，并继续完善公共工具和项目文档。

## Day 2 — 2026 年 7 月 8 日：真实数据实验与代码规范化

### 今日完成

- 使用 Iris 数据集完成 KNN 分类实验，串联数据读取、训练集与测试集划分、特征标准化、模型训练和准确率评估流程。
- 使用 California Housing 样例数据完成 KNN 回归实验，验证 KNN 在连续值预测任务中的使用方式。
- 完善 KNN 的输入检查和异常处理，包括近邻数量、数据维度、样本数量、空数据以及“预测前必须先训练”等情况。
- 对项目中的 README、注释和 docstring 进行统一整理，使各算法目录的学习目标、建议接口和实现步骤更加清晰。
- 增加基础评估指标和 `BaseEstimator` 相关探索，为后续统一模型接口做准备。
- 通过演示脚本和语法检查排查代码问题，保持仓库中的占位模块也能正常通过 Python 语法检查。

### 遇到的问题与解决

- 从算法子目录直接运行脚本时，出现过无法导入根目录 `utils` 的问题。后续通过从项目根目录运行脚本，并为项目增加可编辑安装配置来改善导入体验。
- 输入验证不完整时，错误数据可能在矩阵运算阶段才触发难以理解的异常，因此将常见边界情况提前检查并给出更明确的错误信息。
- 空的 Python 类或未完成的占位代码会导致仓库级语法检查失败，因此占位内容也需要保持语法完整。

### 今日收获

- 完整走通了“读取数据—预处理—训练—预测—评估”的机器学习实验流程。
- 认识到演示脚本不仅用于展示结果，也可以作为快速发现形状错误、状态错误和边界问题的验证工具。
- 对模型接口设计有了更具体的认识：可以抽取真正通用的能力，但不应为了形式统一而过早建立复杂继承体系。

### 下一步计划

- 开始实现线性回归，重点学习损失函数、梯度计算和参数更新。
- 继续完善模型选择工具，为超参数搜索和交叉验证做准备。

## Day 2 - 2026 年 7 月 9 日：线性模型与模型选择工具

### 今日完成

- 完成线性回归的梯度下降实现，包括权重与偏置初始化、预测、均方误差损失、梯度计算和迭代更新。
- 编写较完整的线性回归演示，验证单特征、多特征、异常输入和不同训练参数下的模型行为。
- 在 `NOTE.md` 中补充梯度下降、线性回归损失函数及偏导数等推导笔记。
- 完成 `KFold`，并改进样本不能被折数整除时的划分逻辑。
- 完成 `GridSearchCV` 的基本实现，通过参数组合、K 折交叉验证和评分比较寻找较优超参数。
- 将网格搜索接入 Iris KNN 实验，用实际模型验证参数搜索流程。
- 重构 KNN 文件结构，将模型和距离函数改为更直观的文件命名；增加 `pyproject.toml`，支持以可编辑方式安装本地项目。
- 完成逻辑回归的初步实现，使用 Sigmoid 函数输出概率，使用交叉熵作为损失函数，并通过梯度下降更新参数。
- 编写逻辑回归演示，补充输入检查和训练过程中的问题修复。
- 更新项目 README 和工具文档，使当前实现进度、运行方式和目录职责更加明确。

### 遇到的问题与解决

- `KFold` 在样本数量不能整除折数时需要合理分配余数，因此调整了每一折的大小，避免遗漏或重复样本。
- `GridSearchCV` 涉及模型初始化、参数组合、重复训练和评分汇总，调试过程中修复了参数设置及数据划分相关问题，并用 KNN 实验进行验证。
- 线性回归和逻辑回归都依赖梯度下降，但两者的预测函数与损失函数不同。通过分别推导均方误差和交叉熵的梯度，加深了对线性模型共性与差异的理解。
- 子目录脚本的导入路径较繁琐，因此加入项目打包配置，减少手动修改 `sys.path` 的需要。

### 今日收获

- 掌握了梯度下降在线性回归和逻辑回归中的基本应用，理解了学习率和迭代次数对收敛过程的影响。
- 理解了逻辑回归虽然名称中包含“回归”，但本质上用于分类，并通过 Sigmoid 将线性输出映射为概率。
- 对交叉验证和网格搜索形成了完整认识：交叉验证负责更稳定地评估模型，网格搜索负责系统比较超参数组合。
- 进一步体会到模型实现、数据处理、模型选择、评估指标和演示脚本共同组成了完整的机器学习实验框架。

### 下一步计划

- 为线性回归、逻辑回归、`KFold` 和 `GridSearchCV` 增加更系统的 pytest 测试。
- 继续检查数值稳定性，例如逻辑回归中的概率裁剪、极端输入以及梯度收敛情况。
- 在完成当前线性模型复盘后，进入决策树的原理学习和手写实现。

## Day 4 — 2026 年 7 月 14 日：决策树原理与 CART 分类树初步实现

### 今日完成

- 学习决策树的基本思想，理解通过不断选择特征和划分数据来构建树形分类规则的过程。
- 学习 ID3 决策树，整理信息熵、条件熵和信息增益的定义及计算公式。
- 了解 ID3 容易偏向取值种类较多的特征，并学习 C4.5 使用信息增益率进行修正的思路。
- 学习 CART 决策树，区分分类树与回归树的划分标准：分类树使用基尼指数，回归树使用平方误差。
- 明确当前项目先实现经典的 CART 二叉分类树，不同时扩展回归树。
- 设计 `Node` 节点结构，用于保存样本数量、划分特征、划分阈值、左右子树和节点预测类别。
- 初步实现 `DecisionTreeClassifier`，搭建 `fit`、`predict`、基尼不纯度计算、最佳特征与阈值搜索、递归建树和单样本预测等主要流程。
- 加入 `max_depth`、`min_sample_split` 和 `min_sample_leaf` 等控制参数，为限制树的复杂度和缓解过拟合做准备。
- 更新决策树实现指引和 `NOTE.md`，记录 ID3、C4.5、CART 之间的联系、区别和重要公式。

### 遇到的问题与思考

- ID3、C4.5 和 CART 的特征选择标准不同，需要先分清信息增益、信息增益率和基尼指数各自解决的问题，再决定代码采用哪一种方案。
- 连续特征不能直接按照类别取值建立多叉树，因此 CART 需要对候选阈值进行遍历，并将数据划分成左右两个子集。
- 决策树的建树过程天然适合使用递归，但必须处理好纯节点、空子集、最大深度、最小划分样本数和找不到有效划分等停止条件，否则容易出现无限递归或无效节点。
- 叶子节点需要保存当前样本中的多数类别，即使后续不能继续划分，也能够给出预测结果。
- 当前代码属于第一版逻辑实现，核心结构已经搭建，但候选阈值生成、基尼概率计算、标签编码、递归停止条件和预测数组构造仍需要通过运行与测试逐项检查。

### 今日收获

- 理解了决策树训练的核心就是反复寻找能够让子节点更纯的划分，并递归地构建左右子树。
- 掌握了熵越大表示数据越混乱、基尼值越小表示数据越纯的直观含义。
- 理解了信息增益率是对信息增益偏好多值特征问题的一种修正，而 CART 则使用二叉划分和基尼指数形成另一套建树方案。
- 将递归、树节点、数据切片和多数表决结合到一个完整模型中，对决策树从公式到代码的映射有了初步认识。

### 下一步计划

- 使用简单的二维分类数据运行第一版 CART 分类树，逐步修正建树和预测过程中的问题。
- 为基尼不纯度、候选阈值、最佳划分、单类别数据和最大深度等关键逻辑编写测试。
- 完善输入数据检查和标签编码，确保字符串标签及非连续数字标签都能正确训练和还原。
- 在分类树稳定后，再学习剪枝方法，并评估是否继续实现 CART 回归树。

## Day 5 — 2026 年 7 月 15 日：完善 CART 分类树并完成 Titanic 实战

### 今日完成

- 对第一版 `DecisionTreeClassifier` 进行集中调试和重构，修正构造函数、基尼不纯度、候选阈值、左右子集划分、多数类计算和预测数组构造等问题。
- 使用相邻特征值的中点作为候选阈值，遍历所有特征和阈值，选择加权 Gini 不纯度最低的有效划分。
- 完善递归建树的停止条件，包括达到 `max_depth`、节点标签已经纯净、样本数不足 `min_samples_split`，以及找不到更优划分等情况。
- 正确实现 `min_samples_leaf`，在接受划分前检查左右子节点的样本数量，防止生成过小或空的叶子节点。
- 为节点保存当前样本的多数类别，使无法继续划分的节点可以直接作为叶子节点进行预测。
- 为决策树增加 `score` 方法，使用准确率评价分类结果。
- 增加训练数据、预测数据和超参数验证，覆盖数据维度、样本数量、特征数量、非法数值、未训练预测和参数范围等情况。
- 编写决策树直接运行演示，检查基尼不纯度、最佳划分、未知样本、字符串标签、纯节点、最大深度、最小叶子样本数和异常输入。
- 使用 Titanic 数据集完成一次较完整的分类实战，包括数据概览、缺失值处理、特征工程、分层划分、模型训练、指标计算和测试集预测。
- 生成 Kaggle 格式的 `titanic_submission.csv`，保存 418 名测试乘客的生存预测结果。
- 学习 CART 回归树的基本原理，并在 `NOTE.md` 中补充回归树笔记。明确回归树使用区域目标均值作为预测值，并以左右区域残差平方和最小作为最佳划分标准。
- 调整 KNN 示例的数据文件位置和读取路径，使算法示例及其数据放在对应目录中。

### 实验结果

- 决策树演示中的 14 项行为和异常检查全部通过。
- Titanic 数据共包含 891 条训练记录，其中 342 人生存，总体生存率为 38.38%。
- 数据中 `Age` 缺失 177 条，`Cabin` 缺失 687 条；年龄使用训练部分的中位数进行填充，并额外保留年龄缺失标记。
- 构造了舱位等级、性别、年龄、家庭人数、是否独自出行和是否有客舱记录等数值特征。
- 手写决策树的训练集准确率为 83.45%，验证集准确率为 80.90%，高于 61.80% 的多数类基线。
- 验证集精确率为 82.69%，召回率为 63.24%，混淆矩阵为 `[[101, 9], [25, 43]]`。
- 最终模型深度为 5，共有 43 个节点和 22 个叶子节点；树中使用较多的特征包括年龄、舱位等级、家庭人数和性别。
- 对 Kaggle 测试集预测出 153 名乘客生存，预测生存率为 36.60%。

### 遇到的问题与解决

- 第一版基尼不纯度错误地使用了类别值而不是类别出现概率。修正后先统计每个类别的样本数，再计算 `1 - sum(p_k^2)`。
- 第一版候选阈值和右子集索引存在错误。修正后使用相邻唯一特征值的中点，并通过布尔掩码统一划分左右数据。
- 递归函数直接返回 `None` 会破坏树结构和预测路径，因此改为先创建保存多数类的叶子节点，只有满足条件时才继续补充左右子树。
- `min_samples_leaf` 不能等到子树建完后再处理，需要在选择划分时提前排除会产生过小叶子的候选阈值。
- Titanic 中同时存在缺失值和类别字段，不能直接交给当前只接收数值特征的决策树，因此单独实现预处理流程，将性别等类别转换为数值，并对年龄缺失进行填充和标记。
- 为避免验证集信息泄漏，年龄中位数只使用开发训练集计算，再用于开发训练集、验证集和 Kaggle 测试集。
- 单看准确率无法完整评价 Titanic 生存预测，因此同时计算精确率、召回率、混淆矩阵，并与多数类基线比较。

### 今日收获

- 从“能够写出决策树的大致结构”推进到“能够正确训练、预测并处理常见边界情况”，进一步理解了公式、递归结构和工程验证之间的关系。
- 认识到候选阈值、停止条件和叶子样本限制会直接决定树结构，也是控制决策树复杂度和过拟合的重要手段。
- 完整实践了真实表格数据中的缺失值处理、特征工程、分层划分、基线比较和多指标评估。
- 理解了训练数据预处理参数只能从训练部分学习，否则会将验证集信息提前泄漏给模型。
- 初步理解 CART 分类树和回归树共享递归二叉划分框架，但优化目标和叶子预测值不同：分类树优化 Gini 并预测多数类，回归树优化残差平方和并预测均值。

## Day 6 — 2026 年 7 月 16 日：集成学习与 AdaBoost 初步实现

### 今日完成

- 继续学习 CART 回归树，整理连续特征的二叉划分过程、叶子节点预测均值和残差平方和最小化目标。
- 开始学习集成学习，理解通过组合多个弱学习器得到强学习器的基本思想。
- 对比 Boosting 与 Bagging：Boosting 让模型串行关注前一轮的错误，主要用于降低偏差；Bagging 通过有放回采样并行训练多个模型，主要用于降低方差。
- 学习随机森林的基本流程，包括 Bootstrap 样本采样、节点级特征随机选择、多棵 CART 树训练，以及分类投票或回归平均。
- 重点学习 AdaBoost，理解样本权重、弱分类器权重以及最终加权投票之间的关系。
- 通过五个学生的二分类示例，手工推演三轮弱分类器训练、加权错误率、模型权重和样本权重更新过程。
- 新增 `09_adaboost/`、`10_gbdt/` 和 `11_xgboost/`，分别补充 AdaBoost、GBDT 和 XGBoost 的学习目标、建议接口、实现顺序及最小验收标准。
- 更新项目目录说明和仓库规则，使当前学习范围从 8 个算法主题扩展到 11 个主题。
- 初步实现 AdaBoost 二分类器和决策树桩弱分类器。
- 实现树桩的特征遍历、相邻特征值中点搜索、左右分类方向比较，以及按当前样本权重计算加权错误率。
- 根据弱分类器错误率计算模型投票权重，并使用指数更新公式提高错分样本权重、降低正确样本权重，随后重新归一化。
- 实现多个弱分类器的加权投票，将累加结果的符号作为最终二分类预测。

### 初步验证

- `09_adaboost/`、`10_gbdt/` 和 `11_xgboost/` 均通过 Python 语法编译检查。
- 使用 6 个一维样本和 `-1/+1` 标签进行最小验证，3 个决策树桩组合后正确预测全部训练样本。
- 三轮模型投票权重约为 `0.805`、`1.099` 和 `0.626`。
- 样本权重更新后总和保持为 1，符合 AdaBoost 每轮归一化的要求。
- 当前结果属于简单数据上的快速检查，尚不能代替完整的边界测试和真实数据实验。

### 遇到的问题与思考

- Boosting 和 Bagging 都会组合多个模型，但模型之间的关系不同：Boosting 的学习器前后依赖，需要逐轮训练；Bagging 的学习器相互独立，可以并行训练。
- AdaBoost 同时存在两套权重：样本权重决定下一轮重点学习哪些数据，模型权重决定最终投票时更相信哪个弱分类器。
- 树桩不仅要寻找最佳特征和阈值，还要比较阈值左右两种分类方向，否则可能错过加权错误率更低的规则。
- AdaBoost 的标准二分类推导通常要求标签使用 `-1` 和 `+1`，后续需要在公开接口中验证标签或自动完成编码与还原。
- 当弱分类器错误率为 0 或 1 时，模型权重公式可能出现除零或无穷大；错误率接近 0.5 时，该分类器也几乎没有提升价值，需要增加数值稳定性和提前停止逻辑。
- 当前实现依赖 `X` 为二维 NumPy 数组，尚未系统检查空数据、非法维度、NaN、特征数量不一致及未训练预测等情况。
- GBDT 与 AdaBoost 都属于 Boosting，但优化思路不同：AdaBoost 直接调整样本权重，GBDT 则逐轮拟合当前损失的负梯度，在平方误差下可以直观理解为拟合残差。

### 今日收获

- 建立了集成学习的整体框架，能够从学习器是否独立、数据如何采样、错误如何反馈以及最终如何组合等角度区分 Bagging 与 Boosting。
- 理解了随机森林通过样本随机性和特征随机性降低树之间的相关性，从而改善单棵决策树容易过拟合的问题。
- 掌握了 AdaBoost 从初始化等权样本、训练弱分类器、计算模型权重、更新样本权重到加权投票的完整流程。
- 将 AdaBoost 数学公式映射到了 NumPy 实现，对布尔掩码、加权错误率和指数权重更新有了更具体的认识。
- 明确了后续学习顺序：先巩固 AdaBoost，再实现回归树和 GBDT，最后在梯度提升基础上理解 XGBoost 的二阶梯度与正则化。

### 下一步计划

- 为 AdaBoost 增加输入验证、标签编码、错误率裁剪和提前停止条件。
- 编写 `demo.py` 和 pytest 测试，验证错分样本权重上升、每轮权重和为 1、模型重复训练状态清空及预测结果形状。
- 使用稍复杂的数据集比较单个决策树桩与 AdaBoost 集成模型的效果。
- 完成 CART 回归树实现，为后续手写 GBDT 提供基础学习器。
- 在 GBDT 能够稳定拟合残差并使训练损失下降后，再进入简化版 XGBoost。

## Day 7 — 2026 年 7 月 17 日：CART 回归树与 GBDT 回归

### 今日完成

- 完成基于平方误差的 CART 回归树 `DecisionTreeRegression`，为后续 GBDT 提供基础学习器。
- 设计回归树节点结构，使内部节点保存划分特征、阈值和左右子树，叶子节点保存当前区域目标值的平均数。
- 实现回归树的候选划分搜索：遍历所有特征，以相邻唯一特征值的中点作为候选阈值。
- 使用左右子区域的残差平方和作为划分误差，选择总平方误差最小的特征与阈值。
- 使用递归构建回归树，并通过 `max_depth` 控制树的最大深度；无法继续划分时，将当前目标均值作为叶子预测值。
- 学习 GBDT 的加法模型、函数空间梯度下降和伪残差，理解平方误差下的负梯度就是“真实值减去当前预测值”。
- 在 `NOTE.md` 中补充 GBDT 的直观示例和数学推导，包括初始模型、负梯度、回归树拟合以及学习率控制下的预测累加。
- 初步实现 `GBDTRegression`，使用目标均值作为初始预测，并按顺序训练多棵回归树拟合残差。
- 实现 GBDT 的预测累加：最终结果由初始均值与每棵回归树预测值乘以学习率后共同组成。
- 修复 GBDT 实现中的模型列表清空、回归树导入、代码格式和 `fit` 返回模型自身等问题。
- 编写 `regression_analysis.py`，使用合成非线性回归数据比较均值基线及不同学习率下的 GBDT 表现。

### 实验结果

- 分析数据共 240 个样本，其中训练集 180 个、测试集 60 个。
- 均值基线的测试 MSE 为 `9.0525`，测试 MAE 为 `2.4081`，测试 R² 为 `-0.0372`。
- 当学习率为 `0.1` 时，GBDT 测试 MSE 为 `7.4281`，测试 R² 为 `0.1489`。
- 当学习率为 `0.3` 时，GBDT 测试 MSE 为 `4.7423`，测试 R² 为 `0.4566`。
- 当学习率为 `1.0` 时，GBDT 的测试 MSE 为 `1.2525`，测试 MAE 为 `0.8903`，测试 R² 为 `0.8565`。
- 在当前固定的合成数据和参数下，学习率 `1.0` 表现最好，相比均值基线测试 MSE 降低了 `86.16%`。
- 最佳配置的训练 MSE 为 `0.1735`，明显低于测试 MSE，说明模型已经出现一定的泛化差距。
- `regression_analysis.py` 创建于 7 月 17 日且能够直接运行，但目前仍是未跟踪文件，尚未提交到 Git。

### 遇到的问题与思考

- 分类树与回归树的整体递归结构相似，但节点纯度和叶子输出不同：分类树使用 Gini 并输出多数类别，回归树使用平方误差并输出区域均值。
- GBDT 不能让每棵树都重复拟合原始目标值；后续树需要拟合当前模型尚未解释的负梯度，在平方误差下就是残差。
- GBDT 的初始预测不能简单设为 0。对于平方误差，能够使初始损失最小的常数是所有目标值的平均数。
- 学习率既影响每棵树对最终结果的贡献，也会改变后续残差的计算。当前实现直接让下一棵树拟合上一棵树的剩余误差，仍需进一步核对学习率是否完整参与每轮残差更新。
- 当前回归树只通过最大深度停止递归，尚未增加最小划分样本数、最小叶子样本数、最小误差下降和输入验证等限制。
- 当前实验中学习率 `1.0` 表现最好，只能说明它适合这组固定数据和参数，不能推广为所有 GBDT 任务的最佳选择。

### 今日收获

- 理解了 CART 回归树本质上是在特征空间中不断寻找使区域内目标值更集中的二叉划分。
- 掌握了回归树从候选阈值、平方误差评估、递归建树到叶子均值预测的完整实现路径。
- 理解了 GBDT 并不是简单平均多棵树，而是将多棵树按顺序相加，让每棵新树修正当前模型的不足。
- 将函数空间梯度下降的数学推导对应到代码中的“初始化预测—计算残差—训练回归树—累加预测”流程。
- 通过均值基线、MSE、MAE 和 R² 的对比，进一步认识到回归实验必须同时关注拟合能力和泛化差距。

### 下一步计划

- 核对 GBDT 训练阶段的残差更新，使学习率正确参与每一轮当前预测和负梯度计算。
- 为 `DecisionTreeRegression` 和 `GBDTRegression` 增加输入验证、未训练检查和超参数验证。
- 将 `regression_analysis.py` 整理后提交，并把关键行为转化为 pytest 测试。
- 记录每轮训练损失，验证增加弱学习器时损失是否整体下降。
- 通过不同树数量、最大深度和学习率组合比较欠拟合、合适拟合与过拟合现象。

## Day 8 — 2026 年 7 月 19 日：XGBoost 原理推导与初步实现

### 今日完成

- 正式提交 GBDT 的非线性回归分析脚本，并将前一天的学习内容补充到每日总结中。
- 开始系统学习 XGBoost，理解它是在梯度提升框架上加入二阶梯度信息和树复杂度正则化的一种提升树算法。
- 在 `NOTE.md` 中补充 XGBoost 的数学推导，从带正则项的目标函数出发，对每轮新增树的损失进行二阶泰勒展开。
- 推导叶子节点的最优权重：
  \[
  w_j^*=-\frac{G_j}{H_j+\lambda}
  \]
  其中 \(G_j\) 和 \(H_j\) 分别表示叶子内一阶梯度与二阶梯度之和。
- 推导节点分裂收益，理解只有收益大于 0 时才值得继续划分；同时明确 `gamma` 用于惩罚新增叶子，`reg_lambda` 用于限制叶子输出幅度。
- 实现 XGBoost 专用回归树 `XGBoostTree`，使节点划分不再使用普通平方误差，而是根据梯度统计量计算分裂收益。
- 为 XGBoost 树实现特征遍历、相邻唯一值中点阈值、最优收益搜索、递归建树和单样本预测。
- 实现基于一阶、二阶梯度的叶子权重计算，并将该权重作为叶子节点的预测输出。
- 初步实现 `XGBoost` 回归模型，使用目标均值作为初始预测，按顺序训练多棵 XGBoost 树。
- 针对平方误差设置一阶梯度为“当前预测减真实值”、二阶梯度为 1，并在每轮训练后更新当前梯度。
- 实现学习率控制下的多棵树预测累加，并修复 XGBoost 中部分梯度更新和冗余代码问题。

### 初步验证

- `11_xgboost/` 通过 Python 语法编译检查。
- 使用固定随机种子生成 240 个双特征非线性回归样本，其中 180 个用于训练、60 个用于测试。
- 使用 30 棵最大深度为 2 的树，设置学习率为 `0.3`、`reg_lambda=1.0`、`gamma=0.0`。
- 模型成功训练出 30 棵树，训练 MSE 为 `0.3533`。
- 测试 MSE 为 `0.9980`，测试 R² 为 `0.9140`。
- 同一测试集上的均值基线 MSE 为 `11.7735`，说明当前实现能够从简单合成数据中学习到明显的非线性规律。
- 上述结果只是快速可运行性验证，尚未覆盖数值边界、参数异常和与标准实现的对照。

### 遇到的问题与思考

- GBDT 的回归树通常直接拟合残差，而 XGBoost 树需要接收每个样本的一阶梯度和二阶梯度，并以目标函数下降量作为划分依据。
- XGBoost 叶子输出不是目标值或残差的简单平均，而是由叶子内梯度总和、Hessian 总和和 L2 正则项共同决定。
- `gamma` 与 `reg_lambda` 的作用不同：`gamma` 直接提高节点继续分裂的门槛，`reg_lambda` 则压缩叶子权重并影响分裂收益。
- 对平方误差而言 Hessian 恒为 1，但保留 Hessian 数组能够让树结构以后扩展到其他可二阶求导的损失函数。
- 每轮新增树输出的是对当前预测的修正量，因此训练阶段梯度更新与预测阶段树结果累加必须使用一致的学习率。
- 当前实现尚未验证空数据、非法维度、NaN、未训练预测、参数范围和训练特征数量变化等情况。
- `predict` 当前按 `n_estimators` 遍历模型，后续若加入提前停止，更稳妥的方式是直接遍历实际保存的 `estimators_`。

### 今日收获

- 理解了 XGBoost 相比基础 GBDT 的关键提升不只是“速度更快”，而是把二阶梯度和模型复杂度显式纳入每轮优化目标。
- 能够从二阶泰勒展开推导叶子最优权重与分裂收益，并将公式对应到 NumPy 代码。
- 掌握了 XGBoost 的基本训练链路：初始化预测、计算梯度和 Hessian、训练专用树、更新当前预测、累加多棵树。
- 进一步理解了正则化如何直接影响树结构与叶子输出，而不仅仅是训练完成后的附加处理。
- 从 AdaBoost、GBDT 到 XGBoost，逐步建立了对不同 Boosting 算法“错误如何反馈给下一轮学习器”的整体认识。

### 下一步计划

- 为 `XGBoostTree` 和 `XGBoost` 增加输入数据、超参数和拟合状态验证。
- 编写正式的演示与 pytest 测试，单独验证叶子权重、分裂收益、`gamma` 阻止分裂和 `reg_lambda` 压缩叶子输出等行为。
- 记录每轮训练损失，确认增加树时目标函数整体下降，并检查学习率是否一致参与梯度更新。
- 在相同数据划分下比较手写 GBDT 与 XGBoost 的 MSE、R²、树深和泛化差距。
- 增加早停或按实际 `estimators_` 数量预测的机制，为后续更稳健的训练流程做准备。

## Day 9 — 2026 年 7 月 21 日：朴素贝叶斯与 K-Means

### 今日完成

- 学习朴素贝叶斯分类的基本原理，从贝叶斯公式出发理解先验概率、似然概率和后验概率之间的关系。
- 理解“朴素”假设：在类别确定的条件下，各个特征彼此独立，因此联合条件概率可以拆成多个条件概率的乘积。
- 推导朴素贝叶斯的分类规则：比较各类别的 `P(C) × ∏P(x_i | C)`，选择结果最大的类别，不必显式计算对所有类别相同的 `P(X)`。
- 学习拉普拉斯平滑，理解通过给每个离散特征值增加虚拟计数来避免条件概率为 0。
- 初步实现面向离散特征的 `NaiveBayes` 分类器。
- 在训练阶段统计各类别先验概率，并按类别、特征和特征取值保存条件概率表。
- 在预测阶段将先验概率与各特征似然概率相乘，选择联合概率最大的类别。
- 支持通过 `laplace_smoothing_index` 设置拉普拉斯平滑系数。
- 开始学习 K-Means，理解其无监督聚类目标和“分配样本—更新中心”的迭代过程。
- 推导 K-Means 的簇内平方和目标函数，理解 inertia/WCSS 越小说明样本整体越靠近各自的簇中心。
- 初步实现 `KMeans`：随机选择 K 个样本作为初始中心，计算样本到各中心的平方距离，将样本分配给最近中心，再使用簇内均值更新中心。
- 加入固定随机种子、最大迭代轮数、空簇保护和中心收敛检查，并在训练结束后保存聚类中心与训练样本簇标签。
- 学习 K-Means++ 初始化方法，理解让距离现有中心较远的样本以更高概率成为新中心，可以降低初始中心过于集中的风险。
- 学习多次初始化策略：使用不同初始中心独立运行多次，最终保留 inertia 最小的一次结果。

### 初步验证

- `05_naive_bayes/` 和 `06_kmeans/` 均通过 Python 语法编译检查。
- 使用 6 条离散天气数据快速验证朴素贝叶斯，模型正确预测全部训练样本，训练准确率为 `100%`。
- 朴素贝叶斯计算得到的类别先验概率之和为 `1.0`。
- 使用两组相距较远的二维样本验证 K-Means，模型正确得到两个各含 3 个样本的簇。
- 两个聚类中心约为 `[5.0, 5.0]` 和 `[0.0333, 0.0333]`。
- 该组数据的最终簇内平方和约为 `0.1933`。
- 上述结果属于最小可运行性检查，尚未覆盖真实数据、不同随机初始化和完整边界条件。

### 遇到的问题与思考

- 当前朴素贝叶斯实现针对离散特征逐值统计概率，并不是使用均值与方差建模连续特征的高斯朴素贝叶斯。
- 如果不使用拉普拉斯平滑，只要某个条件概率为 0，整类样本的联合概率就会被乘成 0，可能掩盖其他有用证据。
- 当前平滑只覆盖训练集中见过的全局特征值；预测时出现训练阶段从未见过的新取值，仍会得到 0 概率，需要设计未知类别的处理方式。
- 多个很小的条件概率直接连乘可能出现浮点下溢，后续可以改用对数概率，将乘法转换为加法。
- K-Means 的结果依赖初始中心，普通随机初始化可能收敛到较差的局部最优解，因此需要 K-Means++ 或多次初始化提高稳定性。
- K-Means 必须预先指定 K，但实际任务中往往不知道合适的簇数，后续可以结合肘部法或轮廓系数进行选择。
- 某轮迭代可能产生没有样本的空簇；当前实现保留该簇旧中心，避免对空数组求均值，但仍可探索重新初始化空簇的策略。
- 当前实现已经学习 K-Means++ 和多次初始化原理，但代码仍使用一次普通随机初始化，不能将笔记中的优化视为已经实现。

### 今日收获

- 掌握了朴素贝叶斯从概率公式到离散条件概率表的实现路径，理解了先验、似然和后验在分类中的具体作用。
- 认识到拉普拉斯平滑不仅是公式修正，而是解决离散概率模型“零概率”问题的重要手段。
- 理解了 K-Means 的核心不是简单计算均值，而是交替优化样本归属与聚类中心，使簇内平方和逐步减小。
- 能够使用 NumPy 的广播思路、`argmin`、布尔掩码和均值运算完成聚类分配与中心更新。
- 对监督分类和无监督聚类的区别有了更直观的认识：朴素贝叶斯依赖标签学习类别概率，而 K-Means 只利用特征结构发现分组。

### 下一步计划

- 为朴素贝叶斯增加输入验证、未训练检查、特征数量检查和未知特征值处理。
- 将朴素贝叶斯预测改为对数概率，避免多个小概率连乘造成数值下溢。
- 为 K-Means 增加 K 值、数据维度、样本数量、NaN 和未训练预测等验证。
- 实现 K-Means++ 与多次初始化，并保存最终 inertia 和实际迭代次数。
- 编写 demo 和 pytest 测试，对比不同随机种子、初始化方法和 K 值下的聚类结果。

## Day 10 — 2026 年 7 月 23 日：PCA、全量复习与项目收官

### 今日完成

- 学习 PCA（Principal Component Analysis，主成分分析），理解它通过重新构造少量互相正交的主成分，在降低特征维度的同时尽可能保留数据中的主要变化信息。
- 复习方差与协方差，理解协方差矩阵用于描述多个特征共同变化的方向和程度。
- 从“投影后方差最大”的目标出发，使用拉格朗日乘数法推导：
  \[
  Cw=\lambda w
  \]
  由此理解主成分就是协方差矩阵的特征向量，对应特征值表示该方向能够解释的方差大小。
- 初步实现 `PCA`，包括数据中心化、协方差矩阵计算、对称矩阵特征分解、特征值降序排列及数据投影。
- 使用 `np.linalg.eigh` 求解实对称协方差矩阵，并选取最大特征值对应的前 `n_components` 个特征向量。
- 对 `NOTE.md` 进行收官整理，恢复并保留 XGBoost 的目标函数、二阶泰勒展开、叶子权重和分裂收益推导，同时修正部分文字与公式格式。
- 调整项目最终目录结构：移除本轮未完成的神经网络目录，将 AdaBoost、GBDT 和 XGBoost 顺延为 `08_adaboost/`、`09_gbdt/` 和 `10_xgboost/`。
- 删除不再需要的 `datasets/README.md`，保留各算法目录内实际使用的数据和分析脚本。
- 从头复习本项目全部内容，重新串联数据预处理、模型评估、监督学习、无监督学习、决策树和集成学习之间的关系。
- 完成整个 `ml-from-scratch` 学习项目，本轮不再继续增加新的算法主题。

### 今日全量复习

- 复习 NumPy 数组维度和 `axis`，重新确认样本矩阵通常使用 `(n_samples, n_features)`，逐样本距离需要沿特征轴聚合。
- 复习数据划分、Min-Max 归一化、Z-Score 标准化、常用回归与分类指标、K 折交叉验证和网格搜索。
- 复习 KNN 分类与回归，理解距离计算、近邻选择、多数投票与邻居目标均值。
- 复习线性回归，重新梳理均方误差、梯度下降、权重与偏置更新，以及学习率对收敛过程的影响。
- 复习逻辑回归，理解 Sigmoid、二分类概率、交叉熵损失和梯度下降之间的联系。
- 复习 ID3、C4.5 和 CART，区分信息增益、信息增益率、Gini 不纯度和回归平方误差等划分标准。
- 复习 CART 分类树与回归树的共同递归结构，以及两者在叶子输出和划分目标上的差异。
- 复习离散朴素贝叶斯，重新理解先验概率、条件独立、似然连乘和拉普拉斯平滑。
- 复习 K-Means 聚类，理解样本分配、中心更新、簇内平方和、K-Means++ 和多次初始化。
- 复习 PCA，理解中心化、协方差矩阵、特征值、特征向量和低维投影。
- 复习集成学习中的 Bagging 与 Boosting，并回顾随机森林的样本随机性和特征随机性。
- 复习 AdaBoost，理解样本权重更新、弱分类器权重和最终加权投票。
- 复习 GBDT，理解初始均值、负梯度/伪残差、回归树拟合与加法模型。
- 复习 XGBoost，理解一阶梯度、二阶梯度、叶子最优权重、分裂收益以及 `gamma`、`lambda` 对树复杂度的约束。

### 收官验收

- `python -m compileall .` 通过，全仓库 Python 文件均可完成语法编译。
- `python -m pytest` 能正常启动，但当前未收集到 pytest 测试用例，因此不能将其表述为“自动化测试全部通过”。
- KNN 演示正常运行，分类预测为 `[0, 1, 0]`，回归预测为 `[1.0, 11.0, 1.0]`。
- 线性回归演示的 `16/16` 项行为与异常检查全部通过。
- 逻辑回归演示正常运行，训练准确率达到 `100%`，训练损失由约 `0.693147` 降至 `0.000626`。
- CART 分类树演示的 `14/14` 项检查全部通过。
- Titanic 分析正常运行，验证集准确率为 `80.90%`，高于 `61.80%` 的多数类基线。
- GBDT 非线性回归分析正常运行，最佳配置测试 R² 为 `0.8565`，相对均值基线测试 MSE 降低 `86.16%`。
- 离散朴素贝叶斯最小验证训练准确率为 `100%`。
- K-Means 最小验证正确得到两个各含 3 个样本的簇。
- PCA 将 3 维相关数据降到 2 维，输出形状为 `(200, 2)`，保留方差比例约为 `99.947%`。
- AdaBoost 最小验证训练准确率为 `100%`。
- XGBoost 非线性回归最小验证测试 R² 约为 `0.8987`。
- 当前实际目录已经调整为 `01_knn/` 至 `10_xgboost/`，但根目录 `README.md` 和 `AGENTS.md` 仍保留旧的神经网络及 `11_xgboost/` 目录描述；若后续对外展示，可再做一次文档同步。

### 项目整体收获

- 从只会调用现成模型，推进到能够把数学公式拆成可执行的 NumPy 步骤，并通过小数据、真实数据和指标验证模型行为。
- 建立了较完整的机器学习基础链路：数据准备、预处理、模型训练、预测、评估、交叉验证和超参数选择。
- 理解了不同算法背后的主要建模思想：基于距离、基于概率、基于梯度优化、基于递归划分、基于降维以及基于弱学习器集成。
- 逐步形成了“先推导原理—再手写核心流程—然后运行演示—最后检查边界”的学习方法。
- 通过 Iris、California Housing、Titanic 和合成非线性数据，将算法从简单单元示例推进到相对完整的数据分析流程。
- 对训练误差、验证误差、基线模型、过拟合、数据泄漏和数值稳定性有了更实际的认识。
- 认识到一个学习实现“能运行”与“具备生产级健壮性”并不相同；输入验证、自动化测试、性能优化和完整 API 仍是更高一层的工程工作。

### 项目完结说明

本轮 `ml-from-scratch` 项目至此完结。最终保留了 KNN、线性回归、逻辑回归、决策树、朴素贝叶斯、K-Means、PCA、AdaBoost、GBDT 和 XGBoost 十个算法主题，并配套实现了数据预处理、模型评估、交叉验证和网格搜索等基础工具。

这个项目的主要目标不是复刻成熟机器学习库的全部能力，而是通过亲自推导和手写核心算法，建立对模型内部计算过程的直觉。从距离、概率和梯度下降，到树模型、降维和 Boosting，已经形成了一条较完整的经典机器学习学习路径。后续如需继续提升，可以把重点从“增加算法数量”转向测试覆盖、数值稳定性、性能优化和统一接口设计，但这些不再属于本轮项目的必做范围。
