# 个人每日学习总结

> 项目：`ml-from-scratch` 机器学习算法手写练习  

##  Day 1  — 2026 年 7 月 7 日：项目搭建与 KNN 初步实现

### 今日完成

- 初始化 `ml-from-scratch` 项目，建立 KNN、线性回归、逻辑回归、决策树、朴素贝叶斯、K-Means、PCA 和神经网络等算法目录。
- 补充项目级说明文档、目录说明和 Git 忽略规则，明确项目以手写算法和理解原理为主。
- 从零实现 KNN 分类与 KNN 回归的核心流程：保存训练数据、计算欧氏距离、寻找最近邻，并分别使用多数投票和均值完成预测。
- 使用 NumPy 调整距离计算及近邻索引逻辑，进一步理解 `axis=0` 和 `axis=1` 对数组运算结果的影响。
- 编写基础演示数据，验证 KNN 分类与回归的预测过程。
- 在 `utils/` 中加入数据集划分、Min-Max 归一化和 Z-Score 标准化等预处理工具。
- 准备 Iris 数据集，为后续真实数据实验打下基础。

### 遇到的问题与解决

- 在计算多个样本之间的欧氏距离时，曾因 `axis` 选择不正确导致输出形状不符合预期。通过检查数组的 `(n_samples, n_features)` 结构，明确了需要沿特征维度聚合。
- KNN 分类与回归拥有相同的距离计算和近邻选择流程，但最终聚合方式不同，因此保留了公共思路，同时分别实现投票和均值预测。
- 不同特征的量纲会直接影响距离结果，因此补充了标准化和归一化工具。

### 今日收获

- 理解了 KNN 是一种“惰性学习”算法，训练阶段主要保存数据，计算工作集中在预测阶段。
- 熟悉了 `np.argsort`、`np.argpartition`、`np.unique` 等 NumPy 方法在近邻搜索和类别统计中的用途。
- 认识到数据预处理并不是独立步骤，而是距离类模型能否得到合理结果的重要前提。

### 下一步计划

- 使用 Iris 等真实数据验证 KNN 分类效果。
- 增加回归数据实验，并继续完善公共工具和项目文档。

## Day 2 — 2026 年 7 月 8 日：真实数据实验与代码规范化

### 今日完成

- 使用 Iris 数据集完成 KNN 分类实验，串联数据读取、训练集与测试集划分、特征标准化、模型训练和准确率评估流程。
- 使用 California Housing 样例数据完成 KNN 回归实验，验证 KNN 在连续值预测任务中的使用方式。
- 完善 KNN 的输入检查和异常处理，包括近邻数量、数据维度、样本数量、空数据以及“预测前必须先训练”等情况。
- 对项目中的 README、注释和 docstring 进行统一整理，使各算法目录的学习目标、建议接口和实现步骤更加清晰。
- 增加基础评估指标和 `BaseEstimator` 相关探索，为后续统一模型接口做准备。
- 通过演示脚本和语法检查排查代码问题，保持仓库中的占位模块也能正常通过 Python 语法检查。

### 遇到的问题与解决

- 从算法子目录直接运行脚本时，出现过无法导入根目录 `utils` 的问题。后续通过从项目根目录运行脚本，并为项目增加可编辑安装配置来改善导入体验。
- 输入验证不完整时，错误数据可能在矩阵运算阶段才触发难以理解的异常，因此将常见边界情况提前检查并给出更明确的错误信息。
- 空的 Python 类或未完成的占位代码会导致仓库级语法检查失败，因此占位内容也需要保持语法完整。

### 今日收获

- 完整走通了“读取数据—预处理—训练—预测—评估”的机器学习实验流程。
- 认识到演示脚本不仅用于展示结果，也可以作为快速发现形状错误、状态错误和边界问题的验证工具。
- 对模型接口设计有了更具体的认识：可以抽取真正通用的能力，但不应为了形式统一而过早建立复杂继承体系。

### 下一步计划

- 开始实现线性回归，重点学习损失函数、梯度计算和参数更新。
- 继续完善模型选择工具，为超参数搜索和交叉验证做准备。

## Day 2 - 2026 年 7 月 9 日：线性模型与模型选择工具

### 今日完成

- 完成线性回归的梯度下降实现，包括权重与偏置初始化、预测、均方误差损失、梯度计算和迭代更新。
- 编写较完整的线性回归演示，验证单特征、多特征、异常输入和不同训练参数下的模型行为。
- 在 `NOTE.md` 中补充梯度下降、线性回归损失函数及偏导数等推导笔记。
- 完成 `KFold`，并改进样本不能被折数整除时的划分逻辑。
- 完成 `GridSearchCV` 的基本实现，通过参数组合、K 折交叉验证和评分比较寻找较优超参数。
- 将网格搜索接入 Iris KNN 实验，用实际模型验证参数搜索流程。
- 重构 KNN 文件结构，将模型和距离函数改为更直观的文件命名；增加 `pyproject.toml`，支持以可编辑方式安装本地项目。
- 完成逻辑回归的初步实现，使用 Sigmoid 函数输出概率，使用交叉熵作为损失函数，并通过梯度下降更新参数。
- 编写逻辑回归演示，补充输入检查和训练过程中的问题修复。
- 更新项目 README 和工具文档，使当前实现进度、运行方式和目录职责更加明确。

### 遇到的问题与解决

- `KFold` 在样本数量不能整除折数时需要合理分配余数，因此调整了每一折的大小，避免遗漏或重复样本。
- `GridSearchCV` 涉及模型初始化、参数组合、重复训练和评分汇总，调试过程中修复了参数设置及数据划分相关问题，并用 KNN 实验进行验证。
- 线性回归和逻辑回归都依赖梯度下降，但两者的预测函数与损失函数不同。通过分别推导均方误差和交叉熵的梯度，加深了对线性模型共性与差异的理解。
- 子目录脚本的导入路径较繁琐，因此加入项目打包配置，减少手动修改 `sys.path` 的需要。

### 今日收获

- 掌握了梯度下降在线性回归和逻辑回归中的基本应用，理解了学习率和迭代次数对收敛过程的影响。
- 理解了逻辑回归虽然名称中包含“回归”，但本质上用于分类，并通过 Sigmoid 将线性输出映射为概率。
- 对交叉验证和网格搜索形成了完整认识：交叉验证负责更稳定地评估模型，网格搜索负责系统比较超参数组合。
- 进一步体会到模型实现、数据处理、模型选择、评估指标和演示脚本共同组成了完整的机器学习实验框架。

### 下一步计划

- 为线性回归、逻辑回归、`KFold` 和 `GridSearchCV` 增加更系统的 pytest 测试。
- 继续检查数值稳定性，例如逻辑回归中的概率裁剪、极端输入以及梯度收敛情况。
- 在完成当前线性模型复盘后，进入决策树的原理学习和手写实现。

## Day 4 — 2026 年 7 月 14 日：决策树原理与 CART 分类树初步实现

### 今日完成

- 学习决策树的基本思想，理解通过不断选择特征和划分数据来构建树形分类规则的过程。
- 学习 ID3 决策树，整理信息熵、条件熵和信息增益的定义及计算公式。
- 了解 ID3 容易偏向取值种类较多的特征，并学习 C4.5 使用信息增益率进行修正的思路。
- 学习 CART 决策树，区分分类树与回归树的划分标准：分类树使用基尼指数，回归树使用平方误差。
- 明确当前项目先实现经典的 CART 二叉分类树，不同时扩展回归树。
- 设计 `Node` 节点结构，用于保存样本数量、划分特征、划分阈值、左右子树和节点预测类别。
- 初步实现 `DecisionTreeClassifier`，搭建 `fit`、`predict`、基尼不纯度计算、最佳特征与阈值搜索、递归建树和单样本预测等主要流程。
- 加入 `max_depth`、`min_sample_split` 和 `min_sample_leaf` 等控制参数，为限制树的复杂度和缓解过拟合做准备。
- 更新决策树实现指引和 `NOTE.md`，记录 ID3、C4.5、CART 之间的联系、区别和重要公式。

### 遇到的问题与思考

- ID3、C4.5 和 CART 的特征选择标准不同，需要先分清信息增益、信息增益率和基尼指数各自解决的问题，再决定代码采用哪一种方案。
- 连续特征不能直接按照类别取值建立多叉树，因此 CART 需要对候选阈值进行遍历，并将数据划分成左右两个子集。
- 决策树的建树过程天然适合使用递归，但必须处理好纯节点、空子集、最大深度、最小划分样本数和找不到有效划分等停止条件，否则容易出现无限递归或无效节点。
- 叶子节点需要保存当前样本中的多数类别，即使后续不能继续划分，也能够给出预测结果。
- 当前代码属于第一版逻辑实现，核心结构已经搭建，但候选阈值生成、基尼概率计算、标签编码、递归停止条件和预测数组构造仍需要通过运行与测试逐项检查。

### 今日收获

- 理解了决策树训练的核心就是反复寻找能够让子节点更纯的划分，并递归地构建左右子树。
- 掌握了熵越大表示数据越混乱、基尼值越小表示数据越纯的直观含义。
- 理解了信息增益率是对信息增益偏好多值特征问题的一种修正，而 CART 则使用二叉划分和基尼指数形成另一套建树方案。
- 将递归、树节点、数据切片和多数表决结合到一个完整模型中，对决策树从公式到代码的映射有了初步认识。

### 下一步计划

- 使用简单的二维分类数据运行第一版 CART 分类树，逐步修正建树和预测过程中的问题。
- 为基尼不纯度、候选阈值、最佳划分、单类别数据和最大深度等关键逻辑编写测试。
- 完善输入数据检查和标签编码，确保字符串标签及非连续数字标签都能正确训练和还原。
- 在分类树稳定后，再学习剪枝方法，并评估是否继续实现 CART 回归树。

## Day 5 — 2026 年 7 月 15 日：完善 CART 分类树并完成 Titanic 实战

### 今日完成

- 对第一版 `DecisionTreeClassifier` 进行集中调试和重构，修正构造函数、基尼不纯度、候选阈值、左右子集划分、多数类计算和预测数组构造等问题。
- 使用相邻特征值的中点作为候选阈值，遍历所有特征和阈值，选择加权 Gini 不纯度最低的有效划分。
- 完善递归建树的停止条件，包括达到 `max_depth`、节点标签已经纯净、样本数不足 `min_samples_split`，以及找不到更优划分等情况。
- 正确实现 `min_samples_leaf`，在接受划分前检查左右子节点的样本数量，防止生成过小或空的叶子节点。
- 为节点保存当前样本的多数类别，使无法继续划分的节点可以直接作为叶子节点进行预测。
- 为决策树增加 `score` 方法，使用准确率评价分类结果。
- 增加训练数据、预测数据和超参数验证，覆盖数据维度、样本数量、特征数量、非法数值、未训练预测和参数范围等情况。
- 编写决策树直接运行演示，检查基尼不纯度、最佳划分、未知样本、字符串标签、纯节点、最大深度、最小叶子样本数和异常输入。
- 使用 Titanic 数据集完成一次较完整的分类实战，包括数据概览、缺失值处理、特征工程、分层划分、模型训练、指标计算和测试集预测。
- 生成 Kaggle 格式的 `titanic_submission.csv`，保存 418 名测试乘客的生存预测结果。
- 学习 CART 回归树的基本原理，并在 `NOTE.md` 中补充回归树笔记。明确回归树使用区域目标均值作为预测值，并以左右区域残差平方和最小作为最佳划分标准。
- 调整 KNN 示例的数据文件位置和读取路径，使算法示例及其数据放在对应目录中。

### 实验结果

- 决策树演示中的 14 项行为和异常检查全部通过。
- Titanic 数据共包含 891 条训练记录，其中 342 人生存，总体生存率为 38.38%。
- 数据中 `Age` 缺失 177 条，`Cabin` 缺失 687 条；年龄使用训练部分的中位数进行填充，并额外保留年龄缺失标记。
- 构造了舱位等级、性别、年龄、家庭人数、是否独自出行和是否有客舱记录等数值特征。
- 手写决策树的训练集准确率为 83.45%，验证集准确率为 80.90%，高于 61.80% 的多数类基线。
- 验证集精确率为 82.69%，召回率为 63.24%，混淆矩阵为 `[[101, 9], [25, 43]]`。
- 最终模型深度为 5，共有 43 个节点和 22 个叶子节点；树中使用较多的特征包括年龄、舱位等级、家庭人数和性别。
- 对 Kaggle 测试集预测出 153 名乘客生存，预测生存率为 36.60%。

### 遇到的问题与解决

- 第一版基尼不纯度错误地使用了类别值而不是类别出现概率。修正后先统计每个类别的样本数，再计算 `1 - sum(p_k^2)`。
- 第一版候选阈值和右子集索引存在错误。修正后使用相邻唯一特征值的中点，并通过布尔掩码统一划分左右数据。
- 递归函数直接返回 `None` 会破坏树结构和预测路径，因此改为先创建保存多数类的叶子节点，只有满足条件时才继续补充左右子树。
- `min_samples_leaf` 不能等到子树建完后再处理，需要在选择划分时提前排除会产生过小叶子的候选阈值。
- Titanic 中同时存在缺失值和类别字段，不能直接交给当前只接收数值特征的决策树，因此单独实现预处理流程，将性别等类别转换为数值，并对年龄缺失进行填充和标记。
- 为避免验证集信息泄漏，年龄中位数只使用开发训练集计算，再用于开发训练集、验证集和 Kaggle 测试集。
- 单看准确率无法完整评价 Titanic 生存预测，因此同时计算精确率、召回率、混淆矩阵，并与多数类基线比较。

### 今日收获

- 从“能够写出决策树的大致结构”推进到“能够正确训练、预测并处理常见边界情况”，进一步理解了公式、递归结构和工程验证之间的关系。
- 认识到候选阈值、停止条件和叶子样本限制会直接决定树结构，也是控制决策树复杂度和过拟合的重要手段。
- 完整实践了真实表格数据中的缺失值处理、特征工程、分层划分、基线比较和多指标评估。
- 理解了训练数据预处理参数只能从训练部分学习，否则会将验证集信息提前泄漏给模型。
- 初步理解 CART 分类树和回归树共享递归二叉划分框架，但优化目标和叶子预测值不同：分类树优化 Gini 并预测多数类，回归树优化残差平方和并预测均值。

## Day 6 — 2026 年 7 月 16 日：集成学习与 AdaBoost 初步实现

### 今日完成

- 继续学习 CART 回归树，整理连续特征的二叉划分过程、叶子节点预测均值和残差平方和最小化目标。
- 开始学习集成学习，理解通过组合多个弱学习器得到强学习器的基本思想。
- 对比 Boosting 与 Bagging：Boosting 让模型串行关注前一轮的错误，主要用于降低偏差；Bagging 通过有放回采样并行训练多个模型，主要用于降低方差。
- 学习随机森林的基本流程，包括 Bootstrap 样本采样、节点级特征随机选择、多棵 CART 树训练，以及分类投票或回归平均。
- 重点学习 AdaBoost，理解样本权重、弱分类器权重以及最终加权投票之间的关系。
- 通过五个学生的二分类示例，手工推演三轮弱分类器训练、加权错误率、模型权重和样本权重更新过程。
- 新增 `09_adaboost/`、`10_gbdt/` 和 `11_xgboost/`，分别补充 AdaBoost、GBDT 和 XGBoost 的学习目标、建议接口、实现顺序及最小验收标准。
- 更新项目目录说明和仓库规则，使当前学习范围从 8 个算法主题扩展到 11 个主题。
- 初步实现 AdaBoost 二分类器和决策树桩弱分类器。
- 实现树桩的特征遍历、相邻特征值中点搜索、左右分类方向比较，以及按当前样本权重计算加权错误率。
- 根据弱分类器错误率计算模型投票权重，并使用指数更新公式提高错分样本权重、降低正确样本权重，随后重新归一化。
- 实现多个弱分类器的加权投票，将累加结果的符号作为最终二分类预测。

### 初步验证

- `09_adaboost/`、`10_gbdt/` 和 `11_xgboost/` 均通过 Python 语法编译检查。
- 使用 6 个一维样本和 `-1/+1` 标签进行最小验证，3 个决策树桩组合后正确预测全部训练样本。
- 三轮模型投票权重约为 `0.805`、`1.099` 和 `0.626`。
- 样本权重更新后总和保持为 1，符合 AdaBoost 每轮归一化的要求。
- 当前结果属于简单数据上的快速检查，尚不能代替完整的边界测试和真实数据实验。

### 遇到的问题与思考

- Boosting 和 Bagging 都会组合多个模型，但模型之间的关系不同：Boosting 的学习器前后依赖，需要逐轮训练；Bagging 的学习器相互独立，可以并行训练。
- AdaBoost 同时存在两套权重：样本权重决定下一轮重点学习哪些数据，模型权重决定最终投票时更相信哪个弱分类器。
- 树桩不仅要寻找最佳特征和阈值，还要比较阈值左右两种分类方向，否则可能错过加权错误率更低的规则。
- AdaBoost 的标准二分类推导通常要求标签使用 `-1` 和 `+1`，后续需要在公开接口中验证标签或自动完成编码与还原。
- 当弱分类器错误率为 0 或 1 时，模型权重公式可能出现除零或无穷大；错误率接近 0.5 时，该分类器也几乎没有提升价值，需要增加数值稳定性和提前停止逻辑。
- 当前实现依赖 `X` 为二维 NumPy 数组，尚未系统检查空数据、非法维度、NaN、特征数量不一致及未训练预测等情况。
- GBDT 与 AdaBoost 都属于 Boosting，但优化思路不同：AdaBoost 直接调整样本权重，GBDT 则逐轮拟合当前损失的负梯度，在平方误差下可以直观理解为拟合残差。

### 今日收获

- 建立了集成学习的整体框架，能够从学习器是否独立、数据如何采样、错误如何反馈以及最终如何组合等角度区分 Bagging 与 Boosting。
- 理解了随机森林通过样本随机性和特征随机性降低树之间的相关性，从而改善单棵决策树容易过拟合的问题。
- 掌握了 AdaBoost 从初始化等权样本、训练弱分类器、计算模型权重、更新样本权重到加权投票的完整流程。
- 将 AdaBoost 数学公式映射到了 NumPy 实现，对布尔掩码、加权错误率和指数权重更新有了更具体的认识。
- 明确了后续学习顺序：先巩固 AdaBoost，再实现回归树和 GBDT，最后在梯度提升基础上理解 XGBoost 的二阶梯度与正则化。

### 下一步计划

- 为 AdaBoost 增加输入验证、标签编码、错误率裁剪和提前停止条件。
- 编写 `demo.py` 和 pytest 测试，验证错分样本权重上升、每轮权重和为 1、模型重复训练状态清空及预测结果形状。
- 使用稍复杂的数据集比较单个决策树桩与 AdaBoost 集成模型的效果。
- 完成 CART 回归树实现，为后续手写 GBDT 提供基础学习器。
- 在 GBDT 能够稳定拟合残差并使训练损失下降后，再进入简化版 XGBoost。
